export interface CreateOrUpdatedynamicPrompts {
    id: any;
    promptText: string;
    type: string; // e.g., "informative", "challenge", etc.
    impact: string; // e.g., "high", "medium", "low"
  }
  